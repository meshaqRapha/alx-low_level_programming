#include <stdio.h>
/**
  * main - program to print its name followed by a new line
  * @argc: argument count
  * @argv: argument vectors
  * Return: 0;
  */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}


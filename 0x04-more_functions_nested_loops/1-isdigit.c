#include "main.h"

/**
  * _isdigit - checks for a digit
  * @c: character to be checked
  * Return: 1 if digit && 0 if otherwise
  */

int _isupper(int c)
{
if (c >= 30 && c <= 39)
{
	return (1);
}

else
{
	return (0);
}
_putchar('\n');
}

#include "main.h"

/**
* main - a function that swaps the values
* of integers a and b
* @a:integer a
* @b:integer b
*Return: Always 0
*/

void swap_int(int *a, int *b);

int main(void)
{
	int temp;

	temp = (*a);
	(*a) = (*b);
	(*b) = temp;

}
